import { db } from "@/db";
import { chatsTable, usersTable, usersToChatsTable } from "@/db/schema";
import { eq } from "drizzle-orm";

export const createMessage = async (userId1: string) => {
    "use server"
}

export const updateAnnouncement = async() => {

}